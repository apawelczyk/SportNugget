
@if (isLoading)
{
    if (LoaderTemplate is null)
    {
        <RadzenProgressBar Value="100" ShowValue="false" Mode="ProgressBarMode.Indeterminate" />
    }
    else
    {
        @LoaderTemplate
    }
}
else
{
    @if (ChildContent is null)
    {

    }
    else
    {
        @ChildContent
    }
}